# Miniflux RSS 同步配置文件
# 复制此文件为 config.yaml 并修改配置

miniflux:
  # Miniflux 服务器地址
  host: "https://reader.example.com"
  # API Key (在 Miniflux 设置 -> API Keys 中生成)
  api_key: "your_api_key_here"

# Claude AI 分析配置
claude:
  # 是否启用 AI 分析
  enabled: true
  # API 服务地址 (留空使用官方 API)
  # 自建服务示例: http://your-vps:3000
  base_url: ""
  # API Key
  api_key: "your_api_key_here"
  # 模型选择 (推荐 claude-haiku 性价比高)
  # 可选: claude-haiku-4-5, claude-sonnet-4-5, claude-opus-4-5
  model: "claude-opus-4-5"

sync:
  # 临时保存路径 (脚本生成到这里，rclone 再从这里搬走)
  temp_path: "/root/rss_sync/temp_inbox"
  # 每次获取的最大文章数
  limit: 50
  # 同步后是否取消收藏
  unstar_after_sync: true

# rclone 云端同步配置
rclone:
  # 是否启用 rclone 同步
  enabled: true
  # 目标 remote 路径 (格式: remote_name:path)
  # 使用 rclone listremotes 查看已配置的 remote 名称
  # 示例: onedrive:/Documents/Obsidian/Inbox
  remote: "onedrive:/path/to/your/obsidian/inbox"

# 日志配置
logging:
  # 日志文件路径 (留空则只输出到控制台)
  file: "/var/log/rss_sync.log"
  # 日志级别: DEBUG, INFO, WARNING, ERROR
  level: "INFO"
